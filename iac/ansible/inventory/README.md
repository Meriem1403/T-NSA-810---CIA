Copier `example.inventory.yml` :

```bash
cp example.inventory.yml production.yml
```

Le fichier `production.yml` est ignoré par Git (`../../.gitignore`). Ne pas y mettre de secrets en clair si versionné anomalie.
