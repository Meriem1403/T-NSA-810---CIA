---
# Point d’entrée : découper ensuite en rôles (openvpn netbox bastion elk-agent…)
- name: CIA baseline — exemple de collecte infos (à étendre)
  hosts: all
  gather_facts: true
  tasks:
    - name: Vérifier connectivité
      ansible.builtin.ping:
