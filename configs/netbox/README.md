# NetBox — IPAM

- Scripts `curl` / Python appelant l’API avec **token en variable d’environnement**.
- Modèles de préfixes / sites JSON importables (sans données sensibles client réel).
- Playbook Ansible sync : à placer idéalement sous `iac/ansible/roles/netbox-sync/`.
